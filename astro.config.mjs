// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Set this to your production URL once you deploy (used for sitemaps, canonical URLs, etc.)
  // site: 'https://ftfunding.com',
});
